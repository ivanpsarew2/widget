include ../components/button
include ../icons/icon-close

mixin bottomSheet(options)
    - options = options || {}
    - const title = options.title
    - const close = options.close !== false
    - const footer = options.footer || null
    - const overlay = options.overlay !== false
    - const extraClass = options.className || ''
    - const sheetClasses = ['bottom-sheet']
    - if (extraClass) sheetClasses.push(extraClass)

    if overlay
        .widget-overlay
            .widget-overlay__backdrop
            div(class=sheetClasses)&attributes(attributes)
                if close
                    button.bottom-sheet__close(type="button")
                        .bottom-sheet__close-icon-wrapper
                            +icon-close('bottom-sheet__close-icon')
                if title
                    .bottom-sheet__title= title
                .bottom-sheet__content
                    block
                if footer
                    .bottom-sheet__footer
                        if footer.primary
                            +button(footer.primary)
                        if footer.secondary
                            +button(footer.secondary)
    else
        div(class=sheetClasses)&attributes(attributes)
            if close
                button.bottom-sheet__close(type="button")
                    +icon-close('bottom-sheet__close-icon')
            if title
                .bottom-sheet__title= title
            .bottom-sheet__content
                block
            if footer
                .bottom-sheet__footer
                    if footer.primary
                        +button(footer.primary)
                    if footer.secondary
                        +button(footer.secondary)
